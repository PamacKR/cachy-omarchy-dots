#!/usr/bin/env bash
# Display a "Done!" message and wait for a keypress before the scratch terminal closes.
echo
gum spin --spinner "globe" --title "Done! Press any key to close..." -- bash -c 'read -n 1 -s'
