[Unit]
Description=Elephant (Walker's data-provider backend)
PartOf=graphical-session.target
After=graphical-session.target

[Service]
ExecStart=/usr/bin/elephant
Restart=on-failure
RestartSec=1

[Install]
WantedBy=graphical-session.target
